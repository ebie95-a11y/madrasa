import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.madrasanurul.app',
  appName: 'Madrasa tul Nurul Mubeen',
  webDir: 'dist/public'
};

export default config;
